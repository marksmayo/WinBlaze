# WinBlaze.Native

Rust cdylib that owns the scanner/runtime boundary and exports a C ABI for the
WinUI 3 / C++/WinRT shell.

Planned contents:

- session lifecycle functions
- scan event callback bridge
- opaque handles for session ownership
- future index/runtime interop points

